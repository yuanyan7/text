<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.td">
<mapper namespace="cn.smbms.dao.flightTickets">
	<select id="getticketsById" resultType="Flighttickets">
		SELECT * FROM flight_tickets WHERE flightId=#{flightId}
	</select>
</mapper>